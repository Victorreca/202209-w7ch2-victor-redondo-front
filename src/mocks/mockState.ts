import { RobotsState } from "../redux/features/robotsSlice/robotsSlice";
import mockRobotList from "./mockRobotsList";

const mockState: RobotsState = {
  list: mockRobotList,
};

export default mockState;
